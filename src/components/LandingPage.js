import React from 'react';

function LandingPage(){
  return(
    <React.Fragment>
      <h1>Welcome to Tech Flashers!</h1>

      {/* if user signed in --> they'll see all the flash cards */}
      {/* else if user is not signed in --> they'll see log-in/sign-up */}

    </React.Fragment>
  );
}

export default LandingPage;